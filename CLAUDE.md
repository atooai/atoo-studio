# Atoo Studio

## Restarting Atoo Studio

- To restart Atoo Studio, run `bash /home/furti/atoo-studio/restart.sh`. Never use any other method (e.g. manually killing processes, running `npm run dev` directly).
- Only restart when the user explicitly asks for a restart. Do not restart proactively after making changes.
